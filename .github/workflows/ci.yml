name: CI

on: [push]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: PHPUnit Tests
        uses: php-actions/phpunit@v4
        with:
          configuration: "phpunit.xml"